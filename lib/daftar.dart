import 'package:flutter/material.dart';

class DaftarPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Daftar'),
      ),
      body: Center(
        child: Text('Ini Halaman Daftar Pertanyaan'),
      ),
    );
  }
}